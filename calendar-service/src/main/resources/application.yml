server:
  port: 8184
  servlet:
    context-path: /app
spring:
  main:
    banner-mode: off
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres?options=--search_path=user_repo
    username: postgres
    password: 5397


  jpa:
    show-sql: false
    open-in-view: false
    hibernate:
      ddl-auto: validate

  jackson:
    default-property-inclusion: non_absent
integration:
  user-service:
    enable: true
service:
  calendar:
    db: true